overwrite: true
schema: "./strapi/schema.graphql"
documents: "queries/**/*.gql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  ./graphql.schema.json:
    plugins:
      - "introspection"
